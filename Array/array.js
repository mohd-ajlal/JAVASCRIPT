// Array in javaScript
// Array is a collection of data

// Whenever we use var or let, we csn store only one value at a time.
// When we feel like sorting multiple values in one variable then instead of using var or let we use array.
// array are the prototype of this class.

// example
// let array = new Array; optional
let array = [1, 2, 3];
console.log(array);
let array2 = [1, "javascript", true, undefined, false];
console.log(array2.length);
console.log(array2);